Step 1: Setting up the HTML Canvas Blueprint

Step 2: Connecting Javascript and Initializing Data Containers
  - create our required global state containers, and build our first vector math function.
  -Before we can instruct the browser to render shapes dynamically 60 times a second, our code needs reference points.
   We need to fetch the canvas element so the script knows where to draw, set up data boxes to hold our coordinates, 
   and build mathematical functions to help compute directional movement later on.

Step 3: Creating the The Game Loop and Rendering the Player.
   - we will write commands to continuously clear old screen drawings and redraw our player character at their tracked coordinate positions
   - video games cannot just draw a picture once and sit still. Games work exactly like a traditional flip-book animation: they update positions
    and redraw everything on the screen roughly 60 times a second.

Step 4: Tracking Keyboard Inputs
- W (up), A (left), S (down), and D (right).
- we also have to keep track of whether each key is currently being held down
-  we can check if a key is held down during every single cycle of mainGameLoop

(In standard programming, when you hold down a keyboard key, there is a small built-in delay before your operating system starts 
repeating the key (like when you hold down a letter while typing a text message). If we used that default behavior,
 our player avatar would move a tiny bit, freeze for a split second, and then start stuttering across the screen!)

Step 5: Activating Real-Time Player Movement
- If the loop checks and finds that a keyPressed is currently true, it will add or subtract a tiny amount from the player's tracking position variables .
-mainGameLoop runs 60 times a second, checking our movement switches inside the loop means that the game instantly calculates new coordinates and re-renders the circle at its brand new location on every single frame.

Step 6: Enforcing Arena Boundaries
-We are going to add a safety check inside our mainGameLoop function.
 Before updating the player's position, we will check if the new position would push the circle past the edges of our canvas boundaries.
 If it does, we will block it right at the border.

Step 7: Tracking Mouse Interactivity for shooting
-Whenever your mouse glides inside our grid, these numbers will automatically adjust to show where your computer pointer is aimed.
-we must figure out the exact point where the player is looking.
Once we know where your mouse pointer is sitting, we can use trigonometry inside our next step to rotate your player's gun barrel directly toward that point!

Step 8: Calculating Aim Angles and Rendering the gun...attactching to player and turning according to mouse
-our coordinate engine knows exactly where your mouse cursor is hovering, we can link it to our graphics layer.
-We will use some basic geometry to calculate the mathematical angle between the center of your player circle and your mouse coordinates.
-we will draw a clean line extending from the center of the player outward, representing a tactical weapon turret barrel pointing directly at your mouse cursor.
-we must recalculate this direction vector dynamically on every single frame inside mainGameLoop.

Step 9: Shooting Projectiles from the Weapon Tip
-To manage multiple projectiles flying around the screen at the same time, we need a flexible data framework. Instead of creating individual variables 
for every single bullet, we treat our single_global_state_object.bullets array like a dynamic array.
-when we click a new dictionary wil, be added
-This package will log three critical parameters: the bullet's current position (starting right at the tip of our gun), and the directional velocities vx and vy
required to make it fly straight along the angle your gun was pointing at the exact instant you clicked.

Step 10: Animating and Rendering Active gun shots
-We will write a simple loop structure that scans through your single_global_state_object.bullets array item by item. 
For every single bullet found in that array, our code will do two things:

Update its position: Add its custom directional speed variables (vx and vy) straight to its coordinates (x and y).

Draw it: Render it onto our grid as a bright glowing red plasma bolt.
-Since your game loop ticks roughly 60 times a second, updating each projectile's position array entry incrementally produces smooth, independent movement paths.

Step 11: Memory for Discharged Bullets
-Right now, when you fire a bullet, it flies off the edge of your screen and disappears visually—but it never stops existing in your computer's memory
-Instead of starting from 0 and going up, we will loop backwards from the end of the array down to 0.
